{
  "name": "playwright-ui-mode-pick-locator-iframe",
  "version": "1.0.0",
  "description": "Simple project to demonstrate iframe limitations",
  "scripts": {
    "start": "http-server -o --cors",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui-port=8082 --ui-host=localhost"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.37.1"
  }
}
